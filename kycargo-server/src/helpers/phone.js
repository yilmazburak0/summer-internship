// const twilio = require('twilio')

const config = require('../config');

export const validatePhoneNumber = async (phone) => {
  // const { TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN } = config.env
  // const twilioClient = twilio(TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN)
  // try {
  //   const lookupNumber = await twilioClient.lookups.phoneNumbers(phone).fetch()
  //   console.log({ lookupNumber })
  // } catch (error) {
  //   console.error(error)
  //   return false
  // }
  // return true
};
